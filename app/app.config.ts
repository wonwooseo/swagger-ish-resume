export default defineAppConfig({
  resume: {
    defaultLocale: 'en',
    locales: [
      { code: 'en', name: 'English', filename: 'en.yaml' },
      { code: 'ko', name: '한국어', filename: 'ko.yaml' },
    ],
  },
});
